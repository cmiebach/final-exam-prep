[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "weathertools"
version = "0.1.0"
description = "A simple weather utilities library for PDA2."
requires-python = ">=3.11"
dependencies = []
